//C Solution:
#include<stdio.h>
int main(){
    char string[123];
    gets(string);

    int length = 0;
    while ( length[string] != 0 ){
        length++;
    }
    printf("%d", length);
    return 0;
}

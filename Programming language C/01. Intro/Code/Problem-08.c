//C Solution:
#include<stdio.h>

int main(){
    int first_num, second_num, last_num;
    scanf("%d %d %d", &first_num, &second_num, &last_num);

    printf("First Value = %d\n",first_num);
    printf("Last Value = %d\n", last_num);
    
    return 0;
}